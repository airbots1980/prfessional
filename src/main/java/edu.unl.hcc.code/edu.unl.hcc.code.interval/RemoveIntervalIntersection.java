package edu.unl.hcc.code.interval;

import java.util.List;

/**
 * Given a collection of intervals, find the minimum number of
 * intervals you need to remove to make
 * the rest of the intervals non-overlapping.
 *
 * <p>Intervals can "touch", such as [0, 1] and [1, 2], but
 * they won't be considered overlapping.
 *
 * <p>For example, given the intervals (7, 9), (2, 4), (5, 8),
 * return 1 as the last interval can be
 * removed and the first two won't overlap.
 *
 * <p>The intervals are not necessarily sorted in any order.
 */

public class RemoveIntervalIntersection {

    public static int findMinCover(List<List<Integer>> intervals) {

        return 0;
    }

    public static void main(String[] args) {

    }
}
